# Docker Compose for Deep Agent example infrastructure.
#
# The default example uses SQLite (no Docker needed).
# Uncomment the services below if you want to use ClickHouse or a real MCP server.
#
# Usage: docker compose -f examples/docker-compose.yml up -d

version: "3.8"

services:
  # Uncomment to use ClickHouse instead of SQLite:
  # clickhouse:
  #   image: clickhouse/clickhouse-server:24.3
  #   ports:
  #     - "8123:8123"
  #     - "9000:9000"
  #   volumes:
  #     - ch-data:/var/lib/clickhouse
  #   environment:
  #     CLICKHOUSE_USER: default
  #     CLICKHOUSE_PASSWORD: ""

  # Placeholder to make the file valid YAML
  placeholder:
    image: alpine:3.19
    command: ["echo", "Deep Agent example infrastructure ready"]

# volumes:
#   ch-data:
